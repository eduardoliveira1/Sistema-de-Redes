type props = {
    
    width: number;
    height: number;
    defaultColor?: string;
    hoverColor?: string;
}

function Button({ height, width }: props) {
  return (
    <>
    <button height={height} width={width} onClick={}>
        
    </button>
    </>
  );
}